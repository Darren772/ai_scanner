# SnapCheck
Press Ctrl+Shift+S -> draw a box -> get grammar/spelling/structure feedback from Gemini.

## Setup
1. Copy `.env.example` to `.env` and paste your Gemini API key
2. `pip install -r requirements.txt`
3. `python main.py`
